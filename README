R CMD INSTALL --preclean --no-multiarch --with-keep.source -l /opt/brave_prod/workspace/package/R /opt/brave_prod/workspace/code/braveR

```
exportPattern("^[[:alpha:]]+")
```
```
roxygen2::roxygenize(".")
```
```
testthat::test_dir("tests/testthat")
```
